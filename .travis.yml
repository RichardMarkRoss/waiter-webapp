language: node_js

env:
     - DATABASE_URL=postgresql://coder:pg123@localhost:5432/waiter_app

node_js:
  - "v8.9.4"

before_script:
  - psql -c 'create database waiter-app;' -U postgres
  - psql -U postgres -d waiter-app -a -f ./scripts/database.sql
  - psql -U postgres -d waiter-app -a -f ./scripts/insert_days.sql
  
addons:
    apt:
    packages:
    - postgresql-server-dev-9.2